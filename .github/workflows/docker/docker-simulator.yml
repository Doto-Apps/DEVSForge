name: Publish Simulator

on:
  push:
    branches:
      - main
    tags:
      - "v*"
    paths:
      - 'simulator/**'
      - 'back/Dockerfile'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Publish devsforge-simulator
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/docker-build
        with:
          image_name: devsforge-simulator
          context: ./
          dockerfile: ./back/Dockerfile
          target: simulator
