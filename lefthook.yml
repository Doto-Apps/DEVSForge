glob_matcher: doublestar
no_auto_install: false

commit-msg:
  scripts:
    "commit-msg.sh":
      runner: bash
pre-commit:
  jobs:
    - name: Format
      run: task format
      stage_fixed: true

    - name: Lint
      run: task lint -- --fix
      stage_fixed: true
