# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#
# pre-push:
#   jobs:
#     - name: packages audit
#       tags:
#         - frontend
#         - security
#       run: yarn audit
#
#     - name: gems audit
#       tags:
#         - backend
#         - security
#       run: bundle audit
#

glob_matcher: doublestar
no_auto_install: false

commit-msg:
  scripts:
    "commit-msg.sh":
      runner: bash
  # commands:
  #   commitlint:
  #     run: npx --yes @commitlint/cli --config .commitlintrc.json --edit {1}

pre-commit:
  parallel: true
  jobs:
    - name: Format
      run: task format
      stage_fixed: true